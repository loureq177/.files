#!/usr/bin/env bash
set -euo pipefail

if ! command -v chromium &>/dev/null; then
    echo "chromium not found" >&2
    exit 1
fi

chromium --ozone-platform-hint=auto \
    --user-data-dir="$HOME/.local/share/pwa/chromium-profile-calendar" \
    --app=https://calendar.google.com

