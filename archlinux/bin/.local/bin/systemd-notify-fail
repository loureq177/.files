#!/usr/bin/env bash

# Download sevice name
SERVICE_NAME="${1:-Unspecified Service}"

notify-send -u critical \
    -a "Systemd" \
    "Błąd usługi systemd" \
    "Usługa <b>${SERVICE_NAME}</b> zakończyła się niepowodzeniem!"
